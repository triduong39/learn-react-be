const todos = require("./Todo/todos");
const todosByUserId = require("./Todo/todosByUserId");
const todo = require("./Todo/todo");
const users = require("./User/users");
const user = require("./User/user");
const login = require("./User/login");

module.exports = {
  todos,
  todosByUserId,
  todo,
  users,
  user,
  login,
};
